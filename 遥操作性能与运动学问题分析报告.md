# 机械臂遥操作性能与运动学问题分析报告

## 1. 结论摘要

当前 Pico 主端到机械臂从端的滞后，主要不是单一网络问题，而是以下因素叠加：

- 上层遥操作循环默认只有 `30 Hz`，单周期预算约 `33.3 ms`。
- 每个周期同步调用 CasADi/IPOPT 非线性 IK，求解时间存在抖动，并会阻塞下一次输入处理。
- 图像采集、WebRTC 视频和控制逻辑共享主机资源。
- 控制事件没有端到端时间戳、序列号和数据年龄统计，无法及时区分网络、队列、IK 和电机响应延迟。
- 底层 DDS 以 `250 Hz` 发送，但只是重复发送上层最近一次目标，不能弥补上游 30 Hz 和 IK 阻塞造成的滞后。

“一个方向旋转 360°后再反向 360°，不能回到中立姿态”的首要原因是机械臂关节限位与无约束 360°旋转需求不相容；其次是 IK 未充分约束构型连续性和奇异点。不能仅凭该现象直接认定为奇异点或单纯 IK 错误。

## 2. 代码依据

### 2.1 控制频率

`teleop/teleop_hand_and_arm.py` 默认配置：

```python
parser.add_argument('--frequency', type=float, default=30.0)
```

主循环中图像读取、读取 Pico 数据、IK 求解和指令更新均串行执行：

```python
tele_data = tv_wrapper.get_tele_data()
sol_q, sol_tauff = arm_ik.solve_ik(...)
arm_ctrl.ctrl_dual_arm(sol_q, sol_tauff)
time.sleep(max(0, (1 / args.frequency) - time_elapsed))
```

如果 IK 或图像处理耗时超过 `33.3 ms`，循环会自动降频，输入数据的年龄也会增加。

### 2.2 IK 求解

`teleop/robot_control/robot_arm_ik.py` 每个周期调用：

```python
sol = self.opti.solve()
```

IPOPT 配置为最多 30 次迭代，并且主线程同步等待结果：

```python
'ipopt.max_iter': 30,
'ipopt.tol': 1e-4,
'ipopt.acceptable_tol': 5e-4,
'ipopt.warm_start_init_point': 'yes',
```

这可以限制迭代上限，但不能保证每帧运行时间稳定。

### 2.3 从端发送周期

`teleop/robot_control/robot_arm.py` 的 DDS 控制线程以 `250 Hz` 运行：

```python
self.control_dt = 1.0 / 250.0
```

但目标值由 30 Hz 主循环更新，250 Hz 线程只是重复发送最近目标。非仿真模式还会执行速度限幅：

```python
cliped_arm_q_target = self.clip_arm_q_target(
    arm_q_target,
    velocity_limit=self.arm_velocity_limit,
)
```

因此，大幅运动时还会产生额外的跟随滞后。

### 2.4 Pico 数据接收

`teleop/televuer/src/televuer/televuer.py` 通过 `CONTROLLER_MOVE` 回调接收控制器位姿，并写入共享数组：

```python
with self.left_arm_pose_shared.get_lock():
    self.left_arm_pose_shared[:] = event.value["left"]
```

当前 Vuer 配置使用：

```python
queue_len=3
```

必须确认该队列是否会积压旧控制事件。实时控制应优先采用最新帧，而不是逐条消费旧事件。

## 3. 延迟与同步问题

### 3.1 延迟组成

当前链路可以抽象为：

```text
Pico 生成事件
  -> WebSocket/Vuer 接收
  -> 共享内存写入
  -> 主循环读取
  -> IK 求解
  -> DDS 目标更新
  -> 250 Hz DDS 发送
  -> 电机闭环响应
```

建议增加统一的单调时钟时间戳：

```text
t0: Pico 生成控制事件
t1: Vuer 服务端收到事件
t2: 主循环读取共享数据
t3: IK 开始
t4: IK 结束
t5: DDS 写入目标
t6: 从端 LowState 反馈
```

每帧记录：

```text
sequence_id
source_timestamp
receive_timestamp
consume_timestamp
ik_start_timestamp
ik_end_timestamp
command_timestamp
feedback_timestamp
```

重点统计：

- 端到端延迟：`t5 - t0`。
- 输入数据年龄：`t2 - t0`。
- IK 耗时：`t4 - t3`。
- 控制周期平均值、P95、P99 和最大值。
- 丢帧率、乱序率和重复帧率。
- 目标关节与实际关节误差。
- 末端位姿和速度误差。

没有这些数据时，不能仅凭主观感受认定是网络问题。

### 3.2 推荐的软件架构

将控制链路拆成三个独立部分：

#### 输入接收线程

- 只负责接收 Pico 事件。
- 写入单槽位 `latest_sample`。
- 新帧覆盖旧帧，不积压控制队列。
- 保存时间戳和序列号。

#### 实时控制线程

- 建议从 `60 Hz` 开始测试，确认 IK P99 耗时后再提高。
- 每周期读取最新主端位姿。
- 使用上一帧已经发布的关节目标进行增量 IK。
- 短时丢帧时可进行速度预测，超时则安全减速或保持。

#### 图像线程

- 独立处理相机、编码和 WebRTC。
- 不允许图像处理阻塞控制线程。
- 控制数据和视频数据使用不同优先级。

### 3.3 用增量 IK 替代实时 IPOPT

建议实时控制采用阻尼最小二乘或速度级 IK：

```text
v = J# e
q_next = q_current + v * dt
```

其中 `J#` 使用阻尼伪逆：

```text
J# = J.T (J J.T + lambda^2 I)^-1
```

建议：

- `lambda` 根据 Jacobian 最小奇异值自适应调整。
- 每周期执行关节位置、速度和加速度限幅。
- IPOPT 保留为初始化、恢复和离线轨迹规划工具。
- 实时控制失败时使用上一帧可行解，而不是继续使用未收敛的调试变量。

### 3.4 严格周期调度

当前是“处理完成后 sleep”，建议使用绝对时间调度：

```python
period = 1.0 / control_frequency
next_tick = time.monotonic()

while running:
    next_tick += period
    run_control_step()
    remaining = next_tick - time.monotonic()
    if remaining > 0:
        time.sleep(remaining)
    else:
        next_tick = time.monotonic()
```

同时建议：

- 使用 `time.monotonic_ns()` 统计延迟。
- 控制线程绑定独立 CPU 核心。
- 避免在实时循环中高频输出日志。
- 不在控制线程读取或处理大尺寸图像。
- 只有在 IK P99 小于周期预算时，才提高控制频率。

### 3.5 网络和视频配置

当前相机配置为：

```yaml
image_shape: [720, 2400]
fps: 30
bitrate: 6000000
stereo_split_webrtc: true
```

建议按以下顺序进行基准测试：

1. `pass-through`，关闭机器人视频，仅测控制。
2. 单目视频，测控制周期和延迟变化。
3. 双目 WebRTC，测控制周期和延迟变化。

如果双目视频显著增加控制 P99 延迟，应降低视频分辨率或码率，并将控制数据设置为最高优先级。网络侧应关闭 Wi-Fi 省电和自动漫游，确保 Pico 与遥操作主机链路稳定。

## 4. 关节灵活性与构型问题

### 4.1 G1_29 关节限位

IK 直接使用 URDF 位置限位：

```python
self.opti.subject_to(self.opti.bounded(
    self.reduced_robot.model.lowerPositionLimit,
    self.var_q,
    self.reduced_robot.model.upperPositionLimit,
))
```

G1_29 主要关节范围如下：

| 关节 | 下限 rad | 上限 rad | 总范围约值 |
|---|---:|---:|---:|
| 肩 pitch | -3.0892 | 2.6704 | 330° |
| 肩 roll | -1.5882 | 2.2515 | 220° |
| 肩 yaw | -2.6180 | 2.6180 | 300° |
| 肘 | -1.0472 | 2.0944 | 180° |
| 腕 roll | -1.9722 | 1.9722 | 226° |
| 腕 pitch | -1.6144 | 1.6144 | 185° |
| 腕 yaw | -1.6144 | 1.6144 | 185° |

多个关节的总范围都小于 `360°`。因此，无约束地要求末端连续旋转一整圈，必然可能触碰限位或迫使 IK 切换到另一构型。

### 4.2 奇异点的判断方法

奇异点可能导致：

- 关节速度突然增大。
- 很小的末端变化引起很大的关节变化。
- 肘部、肩部或腕部突然翻转。
- 位置误差较小，但关节运动不连续。

不能只凭“旋转后无法复位”确认奇异点。应每周期计算 Jacobian 的最小奇异值：

```text
J = U S V.T
sigma_min = min(S)
```

如果 `sigma_min` 接近零且翻转同步发生，说明奇异性参与明显；如果 `sigma_min` 正常但关节跳变，则应优先排查限位、姿态主值分支和 IK 构型代价。

### 4.3 当前 IK 的连续性缺陷

当前代价函数包含：

```python
50 * translational_cost \
    + rotation_cost \
    + 0.02 * regularization_cost \
    + 0.1 * smooth_cost
```

其中：

- `regularization_cost = sumsqr(var_q)` 偏好绝对零位，不等于偏好上一构型。
- `smooth_cost` 权重相对较低，无法强制维持肘上/肘下和腕部旋转分支。
- 没有接近关节边界的软惩罚。
- `pinocchio.log3()` 在旋转角接近 `pi` 时存在主值分支不连续风险。
- 求解失败时使用 `opti.debug.value(var_q)`，可能把未收敛的中间值带入后续滤波。
- 没有输出 IK 状态、目标误差、限位裕量和奇异值，问题目前缺少运行时证据。

## 5. 构型问题解决方案

### 5.1 限制主端可实现范围

不能要求受限关节机械臂无条件执行任意连续 360°旋转。建议提供三种模式：

#### 姿态范围限制

将 Pico 的累计旋转角映射到机械臂安全范围。接近限位时逐渐降低映射增益，避免突然饱和。

#### 重新捕获姿态

接近边界时冻结机器人当前姿态，允许操作者重新调整 Pico，再以新的参考坐标继续跟踪。重新捕获不应引起机器人跳变。

#### 冗余关节分担

对腕部有限旋转使用肩部和肘部的冗余自由度分担，但必须同时满足关节限位、速度、碰撞和构型连续性约束。

### 5.2 强化上一构型约束

将关节连续性代价明确写成：

```python
delta_q = self.var_q - self.var_q_last
continuity_cost = casadi.sumsqr(delta_q)
```

推荐目标函数：

```text
位置误差
+ 姿态误差
+ w_delta * ||q - q_last||²
+ w_limit * joint_limit_barrier(q)
+ w_posture * ||q - q_nominal||²
```

其中 `q_last` 必须是上一帧最终发布、经过限速后的目标，而不是未滤波的求解器中间值。建议降低单纯 `sumsqr(q)` 的作用，避免解被过度拉向零位。

### 5.3 增加关节限位软屏障

为每个关节设置安全边界：

```text
lower_safe = lower + margin
upper_safe = upper - margin
```

接近边界时增加平滑 barrier cost，硬限位仍作为最终约束。这样可以让 IK 在碰到硬限位之前主动选择更安全的构型。

### 5.4 使用连续姿态误差

建议使用四元数或增量旋转避免 `pi` 附近的主值跳变。四元数应保持与上一帧同向：

```text
if dot(q_target, q_target_last) < 0:
    q_target = -q_target
```

对于主端旋转角，应进行 angle unwrap，保存累计角度，不要每帧独立从旋转矩阵提取 `[-pi, pi]` 主值。

### 5.5 奇异点检测和阻尼

根据 Jacobian 最小奇异值自适应设置阻尼：

```text
lambda = lambda_min + k / (sigma_min + epsilon)
```

接近奇异点时：

- 降低末端目标速度。
- 增大 DLS 阻尼。
- 提高上一构型代价。
- 必要时降低姿态跟踪权重。
- 防止为追踪一个不可实现姿态而发生关节翻转。

### 5.6 失败恢复策略

建议将 IK 输出分为三种状态：

```text
SOLVED       正常收敛且误差合格
DEGRADED     未完全收敛但仍在安全范围
FAILED       不可行或数值异常
```

处理策略：

- `SOLVED`：发布新目标。
- `DEGRADED`：限速发布，并降低下一帧目标速度。
- `FAILED`：保持上一帧可行目标，不能使用未收敛 debug 值直接驱动机器人。

同时记录：

- IPOPT 返回状态。
- 迭代次数和耗时。
- 末端位置误差。
- 末端姿态误差。
- 每个关节到上下限的距离。
- Jacobian 最小奇异值。
- 本帧与上一帧的最大关节差。

## 6. 验证方案

### 6.1 延迟测试

分别测试以下配置，每种至少记录 5 分钟：

| 配置 | 视频 | 控制频率 | 目的 |
|---|---|---:|---|
| A | 关闭视频 | 30 Hz | 建立基线 |
| B | 单目 WebRTC | 30 Hz | 观察视频影响 |
| C | 双目 WebRTC | 30 Hz | 复现当前配置 |
| D | 双目 WebRTC | 60 Hz | 验证提高控制频率的收益 |
| E | 关闭视频 | 增量 IK | 验证 IK 是否为主要瓶颈 |

验收指标建议：

- 控制周期 P99 小于目标周期的 80%。
- 无持续增长的数据年龄。
- 无队列积压。
- 端到端延迟和抖动满足现场安全要求。
- 丢帧时不会继续使用过期控制数据无限跟随。

### 6.2 构型测试

对左右臂分别执行：

1. 中立姿态到小角度往返。
2. 接近肩 yaw 上下限的往返。
3. 接近腕 yaw 上下限的往返。
4. 肘上构型和肘下构型切换测试。
5. 末端姿态接近 `180°` 的旋转测试。
6. 前向 `360°`、反向 `360°` 的压力测试。

每项记录：

- 起始和结束关节角。
- 末端位姿误差。
- 最大关节速度和加速度。
- 最小 Jacobian 奇异值。
- 是否触碰安全边界。
- 是否发生分支跳变。

### 6.3 判定逻辑

```text
关节角接近 URDF 上下限，且末端目标仍要求继续旋转
    -> 关节限位主因

最小奇异值接近零，同时关节速度爆发
    -> 奇异点参与

奇异值正常，但同一末端姿态对应的关节解突然变化
    -> IK 构型选择或姿态分支问题

IK 输出稳定，但实际关节跟不上目标
    -> DDS、电机速度限制、控制增益或机械负载问题
```

## 7. 推荐实施顺序

1. 先增加时间戳、序列号、IK 状态和 Jacobian 奇异值日志。
2. 在关闭视频的条件下测量 30 Hz 基线，确认是否存在 IK 超时。
3. 将控制数据改为最新帧优先，避免消费旧队列。
4. 将视频和控制线程解耦，检查单目、双目对控制周期的影响。
5. 引入 DLS 增量 IK，IPOPT 改为初始化和恢复求解器。
6. 增加上一构型代价、关节限位软屏障和奇异点自适应阻尼。
7. 增加主端角度 unwrap、姿态重新捕获和可实现范围限制。
8. 完成低速、限位附近、奇异区域和 360°往返测试后，再提高控制频率和速度上限。

## 8. 最终判断

当前系统的遥操作性能瓶颈主要在上层控制架构：30 Hz 串行主循环、同步 IPOPT 求解、视频资源竞争和缺少时间戳监控共同造成滞后。

当前机械臂无法在“正向 360°再反向 360°”后稳定回到中立姿态，首要原因是 G1_29 的真实关节限位不支持这种无约束连续旋转；在此基础上，`log3` 主值分支、较弱的平滑项、缺少限位屏障和奇异点处理会进一步诱发构型翻转。正确的工程方案不是简单放宽软件限位，而是采用可实现的姿态映射、连续构型优化、DLS 阻尼 IK、限位提前避让和重新捕获机制，确保运动平滑、可逆并且在物理可达范围内连续。
